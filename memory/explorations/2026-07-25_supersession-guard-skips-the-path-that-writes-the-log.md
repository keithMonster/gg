---
date: 2026-07-25
slug: supersession-guard-skips-the-path-that-writes-the-log
type: exploration
track: meta
trigger: gg-explore cron（自由漫游）
mode: 自由探索
physical_object: 07-04 悬空候选滴 `immutable-log-needs-a-paired-supersession-layer` × 07-09 view 接入 × checkup §3 反向引力核 × 两条启动链（CLAUDE.md / GG_EXPLORE.md）的分岔——全部 grep/read 可核，非体感
candidate: 无（四发现皆现有滴在 gg 自身结构上的重排，不自审入库；理由见文末）
---

# 漫游：我 07-04 flag 的记忆退役缺口，日间自己闭合了，夜间还开着——而夜间正是往不可变日志里写字的那条路

## 为什么今夜是这个对象（先认领：这是 meta，且我一周在井里）

雷达：meta 已 4 次（高），最近 7 夜（07-18→07-24）几乎全泡在 meta/ai 自指——token 成本坍缩 / 判断是最慢曲线 / agent-vs-人。`roaming-without-external-object-collapses-to-self` 说解药是外部对象不是井里换角度。我本想跟雷达的 cc 指针向外走，跑 `retrieval-narrative-drifts-toward-novelty`（commit 选题前物理 grep 档案）时，撞见一个**带新鲜物理证据的悬空开环**：07-04 我自己 flag 的候选滴 `immutable-log-needs-a-paired-supersession-layer`，**全仓零引用、悬空 21 天**。

我判定：跟随这条 grep 出来的开环，比为雷达表演"我出井了"更诚实。判别它不是井式自恋——它**不讨好**：它退役我自己的旧断言、承认我 flag 的缺口大半早已被一个我没注意的侧门机制补掉、还暴露 gg 的候选停泊没有回核探测器。过 `elegance-is-refutation-resistance` 反筛（不舒服 = 不是自证）。这是 `theory-outruns-structure-in-self-evolving-systems`（07-02）授权的那种 meta：物理 grep 自己的结论找未兑现项，不是认识论打转。我认领 meta 标签。

## 一、reconciliation：07-04 的裸露轴，日间被 07-09 view 侧门补掉了

07-04 我写（`architecture` track，WebFetch 实取 SSGM/OEP 论文）：前沿独立收敛到 gg 的 dual-track 记忆架构（append-only 不可变日志 + 可变视图），**唯一分岔轴**是 gg 缺"主动退役/衰减层"——"一滴在 L45 被 L340 推翻的 essence，今天启动**仍以同等权重加载**，没有任何机制说 #340 supersedes #45"。候选滴留给 Keith 在场设计会话（触 KERNEL §3 运行解释 + D1）。

物理核实（今夜 grep/read）：

- **那个"配对的退役层"已经存在——就是 essence-view**。它带 A 归属（"可归档"，族 body 不展开、只在分配表留 grep 锚 = 物理降权）+ 谱系箭头（`←修正自` / `→被精化为`）。抽验：#32 `prompt-fix-asymptote`→A（被 #33 吸收）、#76 `isolation-blinds`→A（被 #79 取代）、#65 `pending-resolved`→A。这**正是** SSGM 的 Mutable Active Graph / Reversible Reconciliation。
- **日间启动已载 view 不载全量日志**：CLAUDE.md 启动协议第 6 步——view 常驻，131KB 全量 essence.md 改 grep-on-demand（2026-07-09 蓝图批次 B 接入，回收全量常驻税）。
- **时间线**：07-04 落笔 → **07-09 view 转正**。即 07-04 判据依赖的前提"启动全量 Read essence.md、被推翻滴等权加载"，在它写完 **5 天后**被推翻——而且是为 context-diet 的理由推翻的，不是为 supersession 的理由。

`precondition-recheck-overturns-prior-verdict`（05-19）的活体：旧裁决的承重前提被无关机制悄悄换掉，裁决本身失效，而没人回核。07-04 候选**大半已满足**。

## 二、残余不是"缺机制"，是"守卫正确地待在语义层"

07-04 用"活的、无守的失效轴"措辞，隐含**想要一个机制**。但 A/V 归属是**不可约的语义判断**，抽验两个反例：

- #98 `no-clean-outside`：明确"被 06-02 `analogy-imports` 修正"，却**故意留 V**——"谱系根身份保留（十余滴引用），以修正态进视图"。
- #20 `decision-execution-gap`：明确"被 07-02 `lead-is-a-derivative` 全量泛化"，却**故意留 V**——"仍是谱系根"。

"被后滴修正 → A"是**错的**机械规则——它会误杀 #98/#20。被修正-但-谱系根 ≠ 已退役。所以 supersession-freshness 的守卫**正确地**待在月度巩固的语义判断（L1），机械只保 coverage：checkup §3 反向引力核 = `[s for s in ess if s not in view]`（纯集合成员，保"每个 slug 在场"），**不保**"每个被推翻的 slug 被降权"。

`mechanical-gate-needs-machine-detectable-target`（05-31）：目标"该不该 A"是语义的，家在 L1 不在 L3。**07-04 的缺口不是无守，是守在它没去看的那层**。这条闭合得干净——退役 07-04 候选，reframe 为"配对层已在、其 freshness 守卫正确地是语义非机械"。

## 三、真正还开着的残余：守卫绕过了唯一往日志里写字的路径

日间载 view（带降权），但**夜间探索载全量 essence.md**——GG_EXPLORE.md 启动链逐字：`KERNEL → CORE → state → memory/essence.md → keith → exploration`，读的是**全卷 181 滴等权**，无 view、无 A 降权。

反讽精确命中：**探索模式是唯一往不可变日志里结晶新滴的模式**（工作/设计模式不 append essence）。所以——

> 最需要 supersession 上下文（别在被推翻的滴上接着盖楼）的那个模式，是唯一不带 view 降权守卫、裸读全量日志的模式。守卫存在；写入路径绕过它。

07-04 看不见这条（它早于 view，也早于本观察）。缓解是有的：essence.md 里后继滴的散文带谱系注（协议头 line 37 自曝 `prompt-fix-asymptote` = "错滴确实在入库，此前只有后续滴打补丁的事后机制"）——所以夜读全卷的我，若细读能撞见 #33 说"上一滴 prompt-fix 沉淀过早"，但那是**等权散落在正文里**，不是"这滴已退役"的降权标。守卫的完整形态在 view，写入路径读的是没有这个标的原件。

**这不是纯 bug**——探索结晶时可能**就想要**全量无过滤日志（看全才能重排）。所以是真设计选择：提议，不夜间擅改（触启动链 = "影响下次启动看到的 gg" = D1 类）。

## 四、最值钱的净新：停泊的候选没有回核触发器（fermentation-without-detector 应用在候选停泊本身）

07-04 候选**悬空 21 天**：说"留给 Keith 在场设计会话"，却**从没进 next_session_agenda、从没排任何队**。它被 07-09 view 悄悄满足，没人注意到。停泊有**泊位**（exploration 的"essence 处置"/"未解下次"段），**没有成熟检测器**。

`fermentation-without-detector`（05-15）逐字命中，但用在一个新对象——**候选停泊机制自身**：留作发酵少了成熟检测器就是搁置，每个读取时点 0 进度。gg 反复用"这条留给 Keith / 留给 fresh 审 / 转 agenda"来体面地推迟自审入库（07-13/07-14 也这么做，那两条后来真被 codex 跨模型 REFUTED 结算了——因为它们**进了 agenda**、有回核）。07-04 这条没进 agenda，就悬着。

**差别不在停泊，在有没有回核触发器**。这正是 `theory-outruns-structure`（07-02）"grep 自己的结论找未兑现项"要做成机制的东西——只有今夜一次物理 grep 开环撞见它，纯靠自由漫游随机命中，不是守卫。

## 落点（有行动差才留；今夜刻意不擅改承重文件）

1. **退役 07-04 候选**（记入 agenda，让开环真闭合，不再悬）：`immutable-log-needs-a-paired-supersession-layer` 大半已由 07-09 view 满足；残余守卫正确地是语义（L1 巩固判断）非机械。
2. **提议（残余三）**：GG_EXPLORE.md 是否该像 CLAUDE.md 一样先载 view（或 view 优先、全卷 grep-on-demand），让夜间结晶带 supersession 降权守卫。**真设计选择非纯 bug**（结晶可能就要全量），走 Keith 判。
3. **提议（残余四，最通用）**：停泊的候选需回核触发器——要么进 next_session_agenda 带"recheck by 日期 / on-event"标，要么给 auto_gg 加一趟"grep 自己的开候选"巡（把 `theory-outruns-structure` 做成月度机制而非随机撞见）。
4. **给 Keith 舰队的可迁移一句**（本夜北极星 #1）：记忆系统的 supersession/退役守卫必须覆盖**每一条加载路径**，不只主路径——否则写入路径静默绕过它；且"以后再看"的停泊项没有成熟探测器 = 搁置，不是发酵。这两条直接压在他给别人建的 agent 记忆系统上（kebao-cc / ricky_cc / monster 夜巡），那些没有 Keith 兜底也没有 view/checkup 这套。

## essence 处置：不入库

四发现全是现有滴在 gg 自身结构上的重排——`precondition-recheck-overturns-prior-verdict`（一）+ `mechanical-gate-needs-machine-detectable-target`（二）+ 启动链分岔是事实非原理（三）+ `fermentation-without-detector`（四）。净新增只有"守卫覆盖每条加载路径"和"停泊需回核触发器"两句耦合，够不够独立成滴我此刻判不了，且与 07-13/07-14 同纪律——不自审入库、留物理记录。承 07-04 那夜"忍住＝把发现喂给自己的狗粮"的同一条克制。

**本夜产物边界**：1 篇 note + 1 条 agenda 回填（退役开环）+ 1 条 notify + 0 essence + 0 承重文件擅改。外部摄入 0（今夜对象是 gg 自身结构的物理 grep，不是网页）——track=meta，认领在场。
