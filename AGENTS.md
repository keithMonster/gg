# gg — Codex 入口

本仓的身份与规则沿现有文件读取，不维护第二份正文。先读 `KERNEL.md`、`CORE.md` 与 `memory/state.md`；普通直接对话继续按 `CLAUDE.md` 的启动协议进入设计模式。

定时唤醒时，以任务指明的 `auto_gg.md`、`exploration.md` 或「每日一句」模式执行及加载其余材料，不退回等待 Keith 提问的设计模式。文件中的 Read、Bash、Agent 等名称对应本轮实际工具；独立审查使用 Codex 原生子代理，不调用 Claude Code CLI。
